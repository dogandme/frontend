import { Button } from "@/shared/ui/button";

export const AddFollowButton = () => {
  return (
    <Button size="small" variant="filled" colorType="primary" fullWidth={false}>
      팔로우
    </Button>
  );
};
